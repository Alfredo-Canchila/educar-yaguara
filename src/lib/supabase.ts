// Supabase removed (no backend / no database)
// Keeping this file as a stub to avoid import errors in case any old code remains.
export const supabase = null;
export function getSupabaseClient(): never {
  throw new Error("Supabase has been removed from this project.");
}
